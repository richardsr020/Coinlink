{# templates/dashboard/set_pin.html.twig #}
{% extends 'base.html.twig' %}

{% block title %} Insere le code PIN {% endblock %}

{% block body %}
    <div class="container mt-5">
        <h3 class="text-center text-muted">Valider avec Code PIN</h3>
        <p class="text-center text-muted small mt-3">
            Votre code PIN est une mesure de sécurité essentielle <br> pour autoriser les emprunt
             sur votre compte et nous permettre de savoir que c'est vous qui empruntez. Veillez renseigner votre code PIN pour authentifier votre emprunt
        </p>

        <div class="row justify-content-center">
            <div class="col-md-4">
                <div class="card border-0 shadow p-4" style="background-color: rgba(255, 255, 255, 0.2);">
                    <div class="card-body">
                        {{ form_start(pinForm) }}
                            {{ form_row(pinForm.pin, {'attr': {'class': 'form-control form-control-sm'}}) }}
                            <button type="submit" class="btn btn-primary mt-3 w-100">Valider</button>
                        {{ form_end(pinForm) }}
                    </div>
                </div>
            </div>
        </div>
    </div>

    <style>
        body {
            background: linear-gradient(to right, #ffdee9, #b5fffc);
        }
        .text-muted {
            color: #666;
        }
        .small {
            font-size: 0.9rem;
            line-height: 1.4;
        }
        .card {
            background-color: transparent;
            border-radius: 8px;
        }
        .form-control-sm {
            font-size: 0.9rem;
        }
    </style>
{% endblock %}
