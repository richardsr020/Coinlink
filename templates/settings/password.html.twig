{# templates/dashboard/reset_password.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Authenticator{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h3 class="text-center text-muted">Confirmer ce action avec votre mot de passe</h3>
        <p class="text-center text-muted small mt-3">
            Veuillez entrer votre mot de passe pour confirmer cette action. Assurez-vous que le mot de passe 
            saisi correspond à celui utilisé pour votre compte afin de sécuriser la réinitialisation de votre PIN.
        </p>

        <div class="row justify-content-center">
            <div class="col-md-4">
                <div class="card border-0 shadow p-4" style="background-color: rgba(255, 255, 255, 0.2);">
                    <div class="card-body">
                        {{ form_start(passwordForm, {'attr': {'novalidate': 'novalidate'}}) }}
                            {{ form_row(passwordForm.password, {
                                'attr': {
                                    'class': 'form-control form-control-sm',
                                    'placeholder': 'Saisissez votre mot de passe'
                                }
                            }) }}
                            {{ form_row(passwordForm.submit, {
                                'attr': {
                                    'class': 'btn btn-primary mt-3 w-100'
                                }
                            }) }}
                        {{ form_end(passwordForm) }}
                    </div>
                </div>
            </div>
        </div>
    </div>

    <style>
        body {
            background: linear-gradient(to right, #ffdee9, #b5fffc);
        }
        .text-muted {
            color: #666;
        }
        .small {
            font-size: 0.9rem;
            line-height: 1.4;
        }
        .card {
            background-color: transparent;
            border-radius: 8px;
        }
        .form-control-sm {
            font-size: 0.9rem;
        }
    </style>
{% endblock %}
